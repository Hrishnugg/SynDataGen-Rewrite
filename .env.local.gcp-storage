# GCP Storage Bucket Settings\n# Set to true to use mock storage buckets instead of real GCP buckets (for development/testing)\nMOCK_GCP_STORAGE=false\n# Set to true to allow fallback to mock buckets if GCP bucket creation fails\nALLOW_MOCK_FALLBACK=true
