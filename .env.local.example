# Example .env.local file for SynDataGen
# Copy this file to .env.local and fill in your values

# === NextAuth Configuration ===
# Required for authentication
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# === Firebase Configuration ===
# Option 1: Base64-encoded service account (recommended)
# To generate: cat service-account.json | base64
FIREBASE_SERVICE_ACCOUNT=

# Option 2: Individual credential fields
# Only needed if not using FIREBASE_SERVICE_ACCOUNT
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-client-email@example.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour Private Key Here\n-----END PRIVATE KEY-----\n"

# Option 3: Path to service account file
# Only needed if not using options 1 or 2
GOOGLE_APPLICATION_CREDENTIALS=./.firebase/service-account.json

# === Firebase Emulator Configuration ===
# Only for local development without real Firebase
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199

# === Other Configuration ===
# Override default storage bucket (optional)
# FIREBASE_STORAGE_BUCKET=your-storage-bucket.appspot.com

# Enable debug logging (optional)
# DEBUG=true

# Disable auto-mocking in development (optional)
# Set to 'false' to use real Firebase even in development
# MOCK_FIREBASE=false