/**
 * Re-export the ProjectStats component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { default } from '@/features/projects/components/project-detail/project-stats';
