/**
 * Re-export the ProjectJobs component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { default } from '@/features/projects/components/project-detail/project-jobs';
