/**
 * Re-export the DashboardLayoutManager component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { default } from '@/features/dashboard/components/DashboardLayoutManager';
