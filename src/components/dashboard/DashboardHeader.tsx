/**
 * Re-export the DashboardHeader component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { default } from '@/features/dashboard/components/DashboardHeader';
