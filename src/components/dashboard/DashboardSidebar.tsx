/**
 * Re-export the DashboardSidebar component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { default } from '@/features/dashboard/components/DashboardSidebar';
