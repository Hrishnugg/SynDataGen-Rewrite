/**
 * Re-export the DataTable component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { DataTable } from '@/features/data-generation/components/data-viewer/data-table';
