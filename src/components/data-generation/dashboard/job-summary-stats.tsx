/**
 * Re-export the JobSummaryStats component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { JobSummaryStats } from '@/features/data-generation/components/dashboard/job-summary-stats';
