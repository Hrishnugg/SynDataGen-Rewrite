/**
 * Re-export the RateLimitIndicator component from its actual location
 * This maintains backward compatibility with existing imports
 */

export { RateLimitIndicator } from '@/features/data-generation/components/dashboard/rate-limit-indicator';
